import type { GamePayload } from "@/types/api";

export const PAYLOAD: GamePayload = {
  gameId: "local:one",
  gameDate: "2026-07-24",
  words: ["BARCO", "PLUMA", "NOCHE", "ARBOL"],
  mode: "local",
  replayAllowed: true,
};
