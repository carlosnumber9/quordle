---
import { Game } from "@/components/game/Game";
import MainLayout from "@/layouts/MainLayout.astro";

const siteUrl = import.meta.env.PUBLIC_SITE_URL ?? Astro.url.origin;
---

<MainLayout>
  <Game client:load siteUrl={siteUrl} />
</MainLayout>
